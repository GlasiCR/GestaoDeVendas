import styled from "styled-components";

export const Contacts = styled.h5`
    color: #FFF;
    font-weight: 400;
`
const BaseContainer = styled.div`
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
`
export const MainContacts = styled(BaseContainer)`

`
export const ContainerEmail = styled(BaseContainer)`
  margin-left: 10px;
`